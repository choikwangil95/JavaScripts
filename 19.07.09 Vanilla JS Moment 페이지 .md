# Moment Page

## 1 ) RealTime
```javascript
# clock.js

const clockContainer = document.querySelector(".js-clock"),
      clockTitle = clockContainer.querySelector("h1");

function getTime(){
  const date = new Date();
  const minutes = date.getMinutes();
  const hours = date.getHours();
  const seconds = date.getSeconds();

  clockTitle.innerText = `${hours < 10 ? `0${hours}` : hours} : ${minutes < 10 ? `0${minutes}` : minutes}: ${seconds< 10 ? `0${seconds}` : seconds}`
}
// 이것을 작은 if라고 함



function init(){
  getTime();
  setInterval(getTime, 1000);// 이 함수는 함수를 사용할 시간 정해주는거
}

init();
```

```html
# index.html

<body>
  <div class="js-clock">
    <h1> 00:00 </h1>
  </div>
  <script src="index.js"></script>
  <script src="clock.js"></script>
</body>
```

## 2 ) 사용자 정보 입력받고 저장하기
```javascript
# greeting.js

const form = document.querySelector(".js-form")
      ,input = form.querySelector("input")
      ,greeting = document.querySelector(".js-greetings");

const User_LS = "currentUser";
const SHOWING_CN = "showing";

function saveName(text){
  localStorage.setItem(User_LS, text)
}

function handleSubmit(event){ // event가 발생한다면 다른곳으로 가버림
event.preventDefault(); // 근데 이걸 해주면 다른곳으로 가버리는걸 막아줌 금지시킴
const currentValue = input.value;
paintGreeting(currentValue);
saveName(currentValue);
}

function askForName(){
  form.classList.add(SHOWING_CN);
  form.addEventListener("submit", handleSubmit); // form에 뭔가 submit 하면 다음의 함수를 처리할것임.
}

function paintGreeting(text){
  form.classList.remove(SHOWING_CN);
  greeting.classList.add(SHOWING_CN);
  greeting.innerText = `Hello ${text}`;
}

function loadName(){
  const currentUser = localStorage.getItem(User_LS);

  if (currentUser === null){
    askForName();
  }
  else{
    paintGreeting(currentUser);
  }
}

function init(){
  loadName();
}

init();

```

```html
# index.html

 <body>
    <form class="js-form form">
      <input type = "text" placeholder="what is your name?">
    </form>

    <h4 class="js-greetings greetings">  </h4>

    <script src="index.js"></script>
    <script src="clock.js"></script>
    <script src="greeting.js"></script>
 </body>
```
## To do List
```javascript
# TodoList.js

const TodoForm = document.querySelector(".js-ToDoForm"),
      TodoInput = TodoForm.querySelector("input"),
      TodoUl = document.querySelector(".js-UlList");

const List_LS = "List"


function paintTodo(text){
  const li = document.createElement("li");  // li 만들고

  const delBtn = document.createElement("button");  // button 만들고
  delBtn.innerText = "삭제";  // value값 지정

  const span = document.createElement("span");  // span 만들고
  span.innerText = text;  // value값 지정

  li.appendChild(span);   // li 안에 span 넣고
  li.appendChild(delBtn);   // li 안에 delBtn 넣고
  TodoUl.appendChild(li);   // Ul안에 li 넣는다.
}

function handleSubmit(event){
  event.preventDefault();
  const currentValue = TodoInput.value;
  paintTodo(currentValue);
  TodoInput.innerHTML=""; // 초기화?
} 


function LoadList(){
  const List = localStorage.getItem(List_LS);

  if(List !== null){
    
  }
}

function init(){
  LoadList();
  TodoForm.addEventListener("submit", handleSubmit);
}

init();

// 1.만약에 Input값이 있으면 (load 할지 말지) 
// 2.Input으로 submit이라는 event가 생기면 submit을 다뤄줄 함수를 실행시키는데 여기서 input.value를 저장 (submit하는 Event 발생 -> input값을 value로 저장)
// 3. value값과 document.createElement를 이용하여 List를 만들어 준다.
```

```html
# index.html

<body>
    <form class="js-ToDoForm">
      <input type = "text" placeholder="What you gonna do?">
    </form>

    <ul class="js-UlList">
    
    </ul>

    <script src="index.js"></script>
    <script src="clock.js"></script>
    <script src="greeting.js"></script>
    <script src="ToDoList.js"></script>
</body>
```
